# Normalize line endings to LF on commit, check out as-is
* text=auto eol=lf

# Windows scripts explicitly use CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binaries — no line-ending conversion
*.exe binary
*.dll binary
*.so  binary
*.a   binary
