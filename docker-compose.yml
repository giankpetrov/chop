services:
  dev:
    build: .
    volumes:
      - .:/app
      - go-cache:/go/pkg
      - go-build-cache:/root/.cache/go-build
    working_dir: /app

volumes:
  go-cache:
    name: go-pkg-cache
  go-build-cache:
    name: go-build-cache
